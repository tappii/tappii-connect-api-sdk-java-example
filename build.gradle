apply plugin: 'java'
apply plugin: 'application'

repositories {
    mavenCentral()
    maven {
        url 'https://tappii.github.io/maven'
    }
}

dependencies {
    implementation 'link.tappii:tappii-connect-api-sdk-java:0.0.1'
}
